<%= form_for(@summary) do |f| %>
  <% if @summary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@summary.errors.count, "error") %> prohibited this summary from being saved:</h2>

      <ul>
      <% @summary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :raw_text %><br>
    <%= f.text_area :raw_text %>
  </div>
  <div class="field">
    <%= f.label :summarized_text %><br>
    <%= f.text_area :summarized_text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
